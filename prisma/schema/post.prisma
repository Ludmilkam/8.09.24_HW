model Post {
  id              Int       @id @default(autoincrement())
  name            String
  description     String?
  time_publicated Int?
  author          String
  comments        Comment[]
}
